---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<BaseLayout title="文档 — 题域 QuestSphere" description="Excel 导入说明、字段格式、FAQ 与故障排查">
  <Header slot="header" />

  <article class="prose mx-auto max-w-3xl px-4 py-16">
    <h1>使用文档</h1>
    <p>题域是 Android 刷题 App。官网提供导入说明与公开包预览；完整刷题、模考、错题本等功能请在 App 内进行。</p>

    <h2>快速开始</h2>
    <ol>
      <li>准备符合格式要求的 Excel 题库文件（见下方字段说明）。</li>
      <li>打开题域 App → 选择「导入 Excel」→ 指定分类与批次。</li>
      <li>开始顺序练习、模考或错题复习。</li>
      <li>（可选）将题库提交为公开包，供他人在「发现」页订阅。</li>
    </ol>

    <h2>Excel 字段说明</h2>
    <p>题域支持从 DeepTutor 导出或按以下约定自建 Excel（首行为表头）：</p>
    <table>
      <thead>
        <tr>
          <th>列名</th>
          <th>必填</th>
          <th>说明</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>题干</code> / <code>question</code></td>
          <td>是</td>
          <td>题目正文，支持 Markdown</td>
        </tr>
        <tr>
          <td><code>题型</code> / <code>type</code></td>
          <td>是</td>
          <td>如 single（单选）、multiple（多选）、judge（判断）等</td>
        </tr>
        <tr>
          <td><code>选项</code> / <code>options</code></td>
          <td>视题型</td>
          <td>选择题选项，可为 JSON 或多列 A/B/C/D</td>
        </tr>
        <tr>
          <td><code>答案</code> / <code>answer</code></td>
          <td>是</td>
          <td>如 A、AB、true 等</td>
        </tr>
        <tr>
          <td><code>解析</code> / <code>explanation</code></td>
          <td>否</td>
          <td>答案解析，导入后可查看</td>
        </tr>
      </tbody>
    </table>
    <p>
      <strong>模板下载：</strong>标准 Excel 模板将在 App 内测开放时提供。在此之前，可参考 DeepTutor 导出格式或联系内测获取样例文件。
    </p>

    <h2>DeepTutor 导出 → 题域导入</h2>
    <ol>
      <li>在 DeepTutor 中完成个性化出题与筛选。</li>
      <li>导出为 Excel 文件（.xlsx）。</li>
      <li>在题域 App 中选择该文件导入，无需手动改列名（若导出格式已对齐）。</li>
    </ol>

    <h2>发现公开包</h2>
    <p>
      在 App「小组」页上半区浏览公开包，订阅后写入本地分类。官网提供
      <a href="/packs">公开包目录</a> 与分享页 <code>/pack/[packId]</code>，可预览前 3 题。
    </p>

    <h2>故障排查</h2>
    <dl>
      <dt><strong>导入失败：文件格式不支持</strong></dt>
      <dd>请使用 .xlsx 格式；避免 .xls 旧格式或 CSV 直接改后缀。</dd>
      <dt><strong>导入失败：列名无法识别</strong></dt>
      <dd>检查首行表头是否包含「题干」「答案」等必要列，或改用 DeepTutor 标准导出。</dd>
      <dt><strong>导入后乱码</strong></dt>
      <dd>确保 Excel 以 UTF-8 保存；避免特殊符号列名。</dd>
      <dt><strong>公开包订阅后找不到题</strong></dt>
      <dd>订阅写入本地分类 <code>{'public_{packId}'}</code>，请在对应分类下查看。</dd>
    </dl>

    <h2>FAQ</h2>
    <dl>
      <dt><strong>公开包与小组有什么区别？</strong></dt>
      <dd>公开包是只读快照，通过发现页或分享链接订阅；小组需邀请码，成员可协作编辑。</dd>
      <dt><strong>官网能刷题吗？</strong></dt>
      <dd>官网仅提供公开包预览与文档，Web 刷题不在 v1 范围。</dd>
      <dt><strong>App 什么时候可以下载？</strong></dt>
      <dd>
        目前 App 内测筹备中，见 <a href="/download">下载页</a>。公开包预览与文档现已可用。
      </dd>
    </dl>
  </article>

  <Footer slot="footer" />
</BaseLayout>

<style>
  @reference "../../styles/global.css";

  .prose h1 {
    @apply text-3xl font-bold text-slate-900;
  }
  .prose h2 {
    @apply mt-10 text-xl font-semibold text-slate-800;
  }
  .prose p,
  .prose li,
  .prose dd {
    @apply text-slate-600 leading-7;
  }
  .prose ol {
    @apply mt-4 list-decimal space-y-2 pl-5;
  }
  .prose dl {
    @apply mt-4 space-y-4;
  }
  .prose dt {
    @apply text-slate-800;
  }
  .prose code {
    @apply rounded bg-slate-100 px-1.5 py-0.5 text-sm text-blue-700;
  }
  .prose a {
    @apply font-medium text-blue-600 hover:text-blue-700;
  }
  .prose table {
    @apply mt-4 w-full border-collapse text-sm;
  }
  .prose th,
  .prose td {
    @apply border border-slate-200 px-3 py-2 text-left;
  }
  .prose th {
    @apply bg-slate-50 font-medium text-slate-800;
  }
</style>
